const project = (state, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

const projects = (state = [], action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default projects;